@model List<ClinicManagement1.Models.Product>
@*
    if (Model != null)
    {
        foreach (var item in Model)
        {
            <div class="col-sm-6 col-lg-4 text-center item mb-4" data-id="@item.Id">
                @{ if (item.Discount != null)
                    {
                        <span class="tag">Sale</span>
                    }
                }
                <a href="/products/xemchitiet/@item.Id">

                    <img src="~/Assets/images/" alt="Image">
                </a>
                <h3 class="text-dark"><a href="/products/xemchitiet/@item.Id">@item.ProductName</a></h3>

                @{
                    var giacuoi = item.Price_out - item.Discount;
                    @*if (giacuoi > 0)
                        {
                            <label class="discount">Giảm @(String.Format("{0:0,0}", item.Discount))</label>
                        }
                }
                <div class="text-primary h3">
                    @{ if (item.Discount.GetValueOrDefault(0) != 0)
                        {
                            <strong class="text-black">@(String.Format("{0:0,0 vnđ}", giacuoi))</strong>
                            <p class="price text-black"><span><del> @(String.Format("{0:0,0 vnđ}", item.Price_out))</del></span></p>
                        }
                        else
                        {
                            <strong class="text-black">@(String.Format("{0:0,0 vnđ}", item.Price_out))</strong>
                        }
                    }
                </div>
            </div>
        }
    }
*@

<div class="site-section bg-light">
    <div class="container">
        <div class="row">
            <div class="title-section text-center col-12">
                <h2 class="text-uppercase">Hot Products</h2>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12 block-3 products-wrap">
                <div class="nonloop-block-3 owl-carousel">
                    @foreach (var item in Model)
                    {
                        <div class="text-center item mb-4">
                            <a href="/products/xemchitiet/@item.Id"> <img src="@Html.Action("getimage","images", new { id = item.Id})" alt="Image"></a>
                            <h3 class="text-dark"><a href="shop-single.html">@item.ProductName</a></h3>
                            <p class="price">@(String.Format("{0:0,0 vnđ}", item.Price_out))</p>

                        </div>
                    }


                    @*<div class="text-center item mb-4">
            <a href="shop-single.html"> <img src="~/Assets/images/product_01.png" alt="Image"></a>
            <h3 class="text-dark"><a href="shop-single.html">Umcka Cold Care</a></h3>
            <p class="price">$120.00</p>
        </div>

        <div class="text-center item mb-4">
            <a href="shop-single.html"> <img src="~/Assets/images/product_02.png" alt="Image"></a>
            <h3 class="text-dark"><a href="shop-single.html">Umcka Cold Care</a></h3>
            <p class="price">$120.00</p>
        </div>

        <div class="text-center item mb-4">
            <a href="shop-single.html"> <img src="~/Assets/images/product_04.png" alt="Image"></a>
            <h3 class="text-dark"><a href="shop-single.html">Umcka Cold Care</a></h3>
            <p class="price">$120.00</p>
        </div>*@

                </div>
            </div>
        </div>
    </div>
</div>